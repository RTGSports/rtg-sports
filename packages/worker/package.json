{
  "name": "@rtg/worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --remote --port 8787",
    "build": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
